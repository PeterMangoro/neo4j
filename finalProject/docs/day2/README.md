# Day 2 — Canonical extraction

- **Notebook:** Run [AutomotiveSupplyChain_TwoWeekPlan.ipynb](../../AutomotiveSupplyChain_TwoWeekPlan.ipynb) (Week 1 — Day 2 cells) with working directory `finalProject`.
- **Outputs:**
  - `data_export/*.csv` — twelve tables (gitignored; large files stay local).
  - [`extraction_manifest.md`](extraction_manifest.md) — row/column snapshot for audit (committed).

Regenerate the manifest after any re-extraction so the report cites a reproducible snapshot.
