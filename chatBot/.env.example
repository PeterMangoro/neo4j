NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme

# NEO4J_DATABASE=neo4j

# Embedding vector size — must match model output and Neo4j chunk_embedding_idx.
# Python SSOT: app.config.config.embedding_dim (default: app.constants.DEFAULT_EMBEDDING_DIM).
# Regenerate cypher/schema_ddl.cypher after changing: python chatBot/scripts/generate_schema_ddl_cypher.py
EMBEDDING_DIM=768

# OLLAMA_HOST=http://localhost:11434
# OLLAMA_API_KEY=ollama

# Embedding model name as shown by `ollama list` (pull first: ollama pull nomic-embed-text-v2-moe)
# OLLAMA_EMBED_MODEL=nomic-embed-text-v2-moe

# Chat model for Phase 4 baseline RAG (OpenAI-compatible API on Ollama)
# OLLAMA_CHAT_MODEL=llama3.2

# RAG retrieval (optional)
# RAG_TOP_K=8
# RAG_MIN_SCORE=0.35
